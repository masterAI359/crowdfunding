'use client'
// Seller page has its own menu structure, so we don't use the common layout
export default function SellerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

